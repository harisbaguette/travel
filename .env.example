# 친구와 함께 편집하려면 필요하다. 없으면 앱은 혼자 쓰기 모드로 그냥 동작한다
# (핀·일정이 내 브라우저에만 저장됨).
#
# 이 파일을 .env.local 로 복사한 뒤, Neon(https://neon.com)에서 만든 접속 주소를 넣는다.
# 접속 주소는 비밀번호가 들어 있는 비밀이다 — git에 올리지 않는다(.gitignore가 막아 둠).
DATABASE_URL=
