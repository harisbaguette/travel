# 글꼴 출처

- 파일: `MemomentKkukkukk-subset.woff` (메모먼트 꾹꾹체, 한글 subset)
- 가져온 곳: 이 저장소의 `public/fonts/MemomentKkukkukk-subset.woff` (doweek 앱이 쓰던 것과 같은 파일)
- 복사한 날: 2026-08-02
- 불러오는 곳: `design-system/styles/base.css`의 `@font-face` (상대경로 `../assets/fonts/`)
- 쓰는 자리: 디스플레이 전용(`--dw-font-display`) — 앱 이름·화면 제목·빈 화면 제목. 본문에는 쓰지 않는다.

배포 전 라이선스 조건(상업적 이용·재배포 범위)을 원 배포처에서 확인할 것. 이 폴더는 원본을 그대로 복사한 것이며 변형하지 않았다.
