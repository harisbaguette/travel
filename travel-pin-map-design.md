# 여행 핀 지도 앱 — 설계안

> 여행 지역을 정하면 그 주변 지도가 뜨고, 맛집·관광지를 핀으로 꽂는 앱.
> 구글 맵 안 쓰고 무료 지도로. AI가 맛집을 찾아 핀 꽂아줌. 친구랑 같이 핀 공유.
> 나중에 Vercel에 배포.

> ⚠️ 현행(2026-08-08): 지도는 구글 지도로 단일화됨(열쇠 없이 동작, 무료 지도 제거).
> 아래 본문의 "무료 지도(OpenStreetMap/Leaflet)" 설명은 처음 설계 당시의 기록이다.

---

## 큰 그림 (비유)

종이 지도를 펼쳐 놓고, 친구들이랑 같이 보면서 "여기 맛집 좋다더라" 하고 포스트잇을 붙이는 거. 포스트잇은 색깔별로(맛집=빨강, 관광지=파랑). 그 종이 지도를 인터넷에 올려 놓으면, 폰에서도 친구 폰에서도 같은 지도를 볼 수 있고, 누가 포스트잇을 새로 붙이면 다 같이 보임. 거기에 "이 근처 맛집 좀 찾아줘" 하면, 도우미(AI)가 주변을 둘러보고 포스트잇을 대신 붙여줌.

---

## 화면 구성

화면은 크게 세 부분:

1. **지도 화면 (왼쪽 크게)** — 온라인에서 끌어다 쓰는 무료 지도. 손가락으로 드래그·줌인됨. 여기에 핀(포스트잇)이 꽂힘.
2. **사이드바 (오른쪽 좁게)** — 핀 목록이 글로도 나옴. 핀을 누르면 그 장소 설명·사진·메모가 보임.
3. **맨 위 검은 바** — "지역 선택" 칸(예: "오사카")을 고르면 지도가 그 지역으로 슝 이동. 옆에 "AI 맛집 찾아줘" 단추.

---

## 핀 (포스트잇) — 핵심

이게 이 앱의 주인공. 하나하나가 뭘 담는지:

- **종류** — 맛집(🍜), 관광지(📸), 카페(☕), 숙소(🛏), 기타(📍). 색깔도 종류별로 다름.
- **이름** — "오사카 역 앞 라멘집"
- **메모** — "금요일 밤엔 줄 길다 / 꼭 토마토 라멘 시켜라" (내가 쓰는 글)
- **사진** — 마음에 들면 사진 한 장 첨부
- **누가 찍었는지** — 내가 찍었는지 친구가 찍었는지 (나중에 협업하면 구분용)
- **AI가 찾았는지** — AI가 자동으로 찾아 꽂은 건 "AI" 표시가 붙음

핀을 어떻게 꽂냐면:
1. 지도 위 spot을 길게 누르면 거기에 핀 하나 생김
2. 사이드바에서 "장소 추가" 누르면
3. **"AI 찾아줘"** 단추를 누르면 내가 지금 보고 있는 지도 안에서 맛집 몇 개를 찾아 자동으로 꽂아줌

---

## "여행 중 급하게 맛집" — 핵심 기능

사용자가 "이 근처 맛집 급해!" 하면:
- 지도의 지금 보이는 범위(화면 안)를 AI한테 줌
- AI가 그 안에 있는 맛집들을 찾아서 핀으로 자동 꽂음
- 핀에 "이름, 종류(한식/일식/카페), 설명, 왜 추천하는지"가 적혀 있음
- 사용자는 지도에서 가장 가까운 핀을 보고 걸어감

AI가 찾는 방식:
1. 인터넷에서 맛집 정보를 긁어오거나(공개 데이터 사용)
2. 미리 저장해 둔 맛집 목록에서 화면 범위 안에 있는 걸 골라내거나

첫 버전은 ①로, 나중에 정확도 올리고 싶으면 ②를 섞음.

---

## 협업 (같이 핀 꽂기)

- "여행 방"을 만들면 초대 링크가 하나 나옴. 그 링크를 친구한테 주면 친구도 같은 지도에 핀을 꽂을 수 있음.
- 내가 꽂은 핀, 친구가 꽂은 핀이 색깔 테두리로 구분됨 (내 것=초록, 친구=주황 같은 식).
- 누군가 핀을 새로 꽂으면 다른 사람 화면에도 바로 나타남 (실시간).

---

## 저장 단계 (데이터가 어디 들어가나)

- **첫 버전 (지금)** — 브라우저 안 저장. 새로고침해도 살아있음. 이거만 해도 혼자 쓰기엔 충분.
- **두 번째 버전** — 클라우드 저장소. 링크 공유하면 친구도 같은 걸 봄.
- **세 번째 버전** — 실시간 협업. 한 사람이 꽂으면 다른 사람 화면에도 즉시 보임.

---

## 기술 선택 (구글 맵 안 쓰는 이유)

지도 그 자체는 **"OpenStreetMap"** (위키백과처럼 전 세계 사람이 같이 만드는 무료 지도)에서 가져옴. 거기에 지도를 화면에 띄워주는 **"Leaflet"**이란 무료 도구를 얹음. 이 조합이 구글 맵을 안 쓰는 이유 — 구글 맵은 하루에 특정 횟수 넘어가면 돈 내야 하는데, OpenStreetMap + Leaflet은 상업적으로 써도 평생 공짜. 핀 모양·색깔을 내 맘대로 바꾸기도 구글 맵보다 훨씬 자유로움.

| 부품 | 쓸 것 | 왜 |
|------|------|------|
| 지도 엔진 | Leaflet + OpenStreetMap | 무료, 가볍고, Vercel에 올리기 좋음 |
| 프레임워크 | Next.js | Vercel이 이걸 위해 만든 거라 배포가 1분 |
| 저장소 | Supabase | 무료 한도 넉넉, 실시간 협업도 됨 (나중에 붙임) |
| 배포 | Vercel | 깃허브에 올리기만 하면 자동 배포 |

---

## 만드는 순서

1. **지도 한 장 띄우기** — 빈 지도, 줌인/줌아웃만 됨 (30분 안)
2. **핀 꽂기** — 지도 클릭하면 핀 하나 생김, 메모 적기 (1시간 안)
3. **지역 선택** — "오사카" 치면 거기로 날아감 (1시간 안)
4. **저장 살리기** — 새로고침해도 핀이 남음 (30분 안)
5. **AI 맛집 찾기** — "이 근처 맛집 찾아줘" 단추 (가장 기술 있는 부분, 2~3시간)
6. **Vercel 배포** — 인터넷에 올림 (10분 안)
7. **협업** — 초대 링크, 같이 핀 꽂기 (2~3시간, 저장소 필요)

다 합치면 — "내가 여행 가려는 도시를 치면 → 지도가 뜨고 → AI가 맛집을 찾아 핀으로 꽂아주고 → 친구한테 링크 주면 친구도 핀 꽂고 → 폰에서 열어보며 걸어감" — 이게 되는 거.

---

## 정해야 할 것

1. **"여행 방" 단위로 지도를 나눌까, 하나의 큰 지도에 다 몰아넣을까?** (오사카 방, 가오슝 방 각각 vs 하나)
2. **AI 맛집 찾기는 처음부터 넣을까, 1~6단계 깔고 나서 넣을까?**